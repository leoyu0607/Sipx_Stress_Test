sip:
  transport: "udp"          # udp / tcp（先 udp）
  listen_ip: "0.0.0.0"
  listen_port: 5070

registrar:
  addr: "10.100.1.31:5060"  # PBX/交換機
  domain: "10.100.1.31"     # SIP domain（通常跟 PBX IP 或 domain 一樣）
  realm: ""                 # 不知道可留空，收到 401 再用回應的 realm
  expires: 300

agents:
  start_ext: 1001
  count: 50
  password: "P@ssw0rd"
  user_name: "GoAgent/0.1"
